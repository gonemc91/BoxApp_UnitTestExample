<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavComponents2tabsexercise"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        >
       <!-- usesCleartextTraffic for working with http protocol-->

        <activity
            android:name = "com.example.http.app.screens.splash.SplashActivity"
            android:theme="@style/SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.http.app.screens.main.MainActivity"/>

    </application>

</manifest>